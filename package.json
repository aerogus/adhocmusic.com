{
  "name": "adhocmusic.com",
  "version": "1.0.0",
  "description": "Site de l'association AD'HOC",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "stylus": "./node_modules/.bin/stylus --compress app/styles/adhoc.styl --out public/css/adhoc.css",
    "start": "docker-compose up",
    "stop": "docker-compose down",
    "clean": "rm -Rf node_modules vendor volumes"
  },
  "dependencies": {
    "mjml": "4.12.0",
    "stylus": "0.57.0"
  }
}
