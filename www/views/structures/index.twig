{% include 'common/header.twig' %}

<div class="box">
  <header>
    <h1>Liste des structures</h1>
  </header>
  <div>
    <ul>
    {% for structure in structures %}
      <li><img src="{{ structure.picto }}" alt=""> <a href="{{ structure.url }}">{{ structure.name }}</a></li>
    {% endfor %}
    </ul>
  </div>
</div>

{% include 'common/footer.twig' %}
