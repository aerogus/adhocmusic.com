{% include 'common/header.twig' %}

<div class="container">
  <h1 class="px-2 py-1 mb-0 bg-dark text-white rounded-top">Newsletters</h1>
  <div class="border rounded-bottom bg-white p-3">
    <ul class="list-group">
    {% for newsletter in newsletters %}
      <li class="list-group-item"><a href="/newsletters/{{ newsletter.idNewsletter }}">{{ newsletter.title }}</a></td>
    {% endfor %}
    </ul>
  </div>
</div>

{% include 'common/footer.twig' %}
