{% include 'common/header.twig' %}

<div class="container">

{% if unknown_structure %}

<p class="alert alert-danger">Cette structure est introuvable !</p>

{% else %}

  <h1 class="px-2 py-1 mb-0 bg-dark text-white rounded-top">{{ structure.name }}</h1>
  <div class="border rounded-bottom bg-white p-3">
    <table class="table">
      <tr>
        <th>Id</th>
        <td>{{ structure.id }}</td>
      </tr>
      <tr>
        <th>Nom</th>
        <td>{{ structure.name }}</td>
      </tr>
      <tr>
        <th>Imagette</th>
        <td><img src="{{ structure.picto }}" alt=""></td>
      </tr>
      <tr>
        <th>Evénements</th>
        <td>
          <ul>
          {% for event in events %}
            <li><a href="{{ event.url }}">{{ event.date }} : {{ event.name }}</a></li>
          {% endfor %}
          </ul>
        </td>
      </tr>
    </table>
  </div>

{% endif %} {# test unknown structure #}

</div>{# .container #}

{% include 'common/footer.twig' %}
